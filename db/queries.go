package db

// Adders
const addUser = `INSERT INTO USERS(EMAIL, RELEVANT_AIRPORTS, SUBSCRIBED_COUNTRIES, CONFIG) VALUES(@EMAIL, @RELEVANT_AIRPORTS, @SUBSCRIBED_COUNTRIES, @CONFIG)`

// Getters
const findUserQueryId = `SELECT ID, EMAIL, CONFIG, CREATED_AT FROM USERS WHERE ID=$1`
const findUserQueryEmail = `SELECT ID, EMAIL, CONFIG, CREATED_AT FROM USERS WHERE EMAIL=$1`
